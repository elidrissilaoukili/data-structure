#include <iostream>

int main()
{
    // int i = 4;
    // std::cout << i << std::endl;
    // std::cout << (i - 4) << std::endl;
    // std::cout << i / i - 4 << std::endl;

    // if ((i == 3) && (i / (i - 4) == 0))
    // std::cout << "OK";
    // std::cout << "AB";

    std::cout << !(1 && !(0 || 1));
}