#include <stdio.h>
#include <stdlib.h>

int main()
{
     const int year = 22; // age will always be 22
     age = 25;           // assignment of read-only variable 'age'

     printf("Age %d", age);

     return 0;
}